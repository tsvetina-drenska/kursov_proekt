@model List<catalog.Entities.Book>
@{
    ViewData["Title"] = "Книги";
}

<h1>Книги</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Добави книга</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Заглавие</th>
            <th>Автор</th>
            <th>Година</th>
            <th>Ср. оценка</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Year</td>
                <td>
                    @{
                        var avg = book.AverageRating;
                        var fullStars = (int)Math.Floor(avg);
                    }
                    @for (int i = 1; i <= 5; i++)
                    {
                        if (i <= fullStars)
                        {
                            <span style="color: #ffc107;">★</span>
                        }
                        else
                        {
                            <span style="color: #ccc;">★</span>
                        }
                    }
                    <span class="ms-1">(@book.Ratings.Count)</span>
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@book.Id" class="btn btn-sm btn-info">Детайли</a>
                    <a asp-action="Edit" asp-route-id="@book.Id" class="btn btn-sm btn-warning">Редактирай</a>
                    <a asp-action="Delete" asp-route-id="@book.Id" class="btn btn-sm btn-danger">Изтрий</a>
                </td>
            </tr>
        }
    </tbody>
</table>